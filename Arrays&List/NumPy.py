"""
If you want to perform Mathematical calculation then
you should use NumPy arrays
by Importing Numpy Package

Otherwise use list as it work in a Similarway and
more flexible to workwith


Fixed length than using Numpy


"""
# import numpy as np
# a = np.array([[10,30,20],[40,50,60]])
# print(a)

from numpy import array

a = array([10,20,30])
print(a)